/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.pexels.com',
            'media.istockphoto.com',
            'portfolio-x22d.onrender.com',
            'github.com'
        ]
    }
}

module.exports = nextConfig
